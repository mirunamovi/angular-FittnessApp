import { IncorectCredentials } from './incorect-credentials';

describe('IncorectCredentials', () => {
  it('should create an instance', () => {
    expect(new IncorectCredentials()).toBeTruthy();
  });
});
